/** @type {import('jest').Config} */
const config = {
  displayName: {
    "color": "blue",
    "name": "Password Tests"
  },
  expand:true,
  reporters: ['default', 'summary']
};

module.exports = config;